function setup() {
  createCanvas(400, 400);
}

  x = random(int(0,400))
  y = random(int(0,400))
  z = random(int(0,400))

function draw() {
  background(220);
  for i = 0; i < 50; i ++
  triangle (10,10,40,20,30,30)
}
